# Test Command

Run the full test suite with coverage report.

## Steps
1. Run `pytest --tb=short -q` to execute all tests
2. Check coverage report for any drops below 80%
3. If tests fail, analyze the failures and suggest fixes
4. Report summary: passed/failed/skipped counts + coverage %
